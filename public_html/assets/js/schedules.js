$(document).ready(function(){
    /*
        var date = new Date();
        var month = date.getMonth();
        var nMonth = date.getMonth()+1;
        var day = date.getDate();
        var year = date.getFullYear();
        
        var today = nMonth + " " + day;
        var data = []; 
        
        for(var k in nba2016){
            if(nba2016[k].Date == today){
                data.push(nba2016[k]);
            }
        }
        
        for(var k in nhl2016){
            if(nhl2016[k].Date == today){
                data.push(nhl2016[k]);
            }
        }
        
        for(var k in mlb2016){
            if(mlb2016[k].Date == today){
                data.push(mlb2016[k]);
            }
        }

	
	$.ajax({
	    type: "POST", 
	    url: 'ajax_process.php',
	    async:false,
	    data:{
	    	d: data,
	    	scores: true
	    } ,
	    success:function(response){
	    	$(".slider4").append(response);
	    }
	});

	$('.slider4').bxSlider({
	    slideWidth: 220,
	    minSlides: 2,
	    maxSlides: 5,
	    moveSlides: 1,
	    slideMargin: 10
	});

	$(".bx-pager").hide();
        */
});